# Copie: cp .env.example .env
STACK_VERSION=9.4.3
ELASTIC_PASSWORD=onp-elastic-2026!
KIBANA_PASSWORD=onp-kibana-2026!
# Gere com: docker exec -it es-onp bin/elasticsearch-service-tokens create elastic/fleet-server token-onp
FLEET_SERVER_SERVICE_TOKEN=
# Copie do Kibana: Fleet > Agent policies > "ONP - Hosts" > Add agent > enrollment token
HOST_ENROLLMENT_TOKEN=
