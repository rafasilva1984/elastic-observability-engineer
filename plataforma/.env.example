# Copie para .env  ->  cp .env.example .env
# Senhas de LAB (descartáveis). Não use senhas reais aqui.
STACK_VERSION=9.4.3
ELASTIC_PASSWORD=onp-elastic-2026
KIBANA_PASSWORD=onp-kibana-2026
APM_SECRET_TOKEN=onp-apm-token-2026

# Preenchidos AUTOMATICAMENTE por ./scripts/subir.sh — não edite à mão.
FLEET_SERVER_SERVICE_TOKEN=
AGENT_ENROLLMENT_TOKEN=
